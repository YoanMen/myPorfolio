<section aria-label="section à propos" id="propos" class="max-w-screen-sm mx-auto sm:rounded-md bg-secondary sm:border-[1px] border-stroke p-4 mb-32 max-lg:mb-20 font-eudoxus">

	{% if is_granted("ROLE_USER") %}
		<div class="flex justify-between items-center">
			<h2 class="text-2xl mb-4 font-bold text-white">A propos</h2>
			<a id="modification" aria-label="boutton de modification" class="hover:text-white transition-colors duration-150" href="{{ url("app_admin.about") }}" data-turbo="false">modifier</a>

		</div>
	{% else %}

		<h2 class="text-2xl mb-4 font-bold text-white">A propos</h2>
	{% endif %}
	<p class="leading-8">{{ about | raw }}
	</p>
</section>
